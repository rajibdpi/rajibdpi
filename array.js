var bag = ["Chal", "Dal", "Ata", "Saban", "Apple", "Orange", "Plum", "1BHK", "2BHK", "3BHK", "4BHK"];

for (var i = 0; i < bag.length; i++) {
    const x = bag[i];
    console.log(`Item is ${i + '-->' + x}`);
}